require('fidget').setup {
    notification = {window = {avoid = {"NvimTree", "nvim-tree.lua"}}}
    --[[
     [ integration = {
     [   ["nvim-tree"] = {
     [     enable = true,
     [   }
     [ }
     ]]
}
